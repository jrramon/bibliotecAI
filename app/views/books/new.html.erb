<section class="page book-new">
  <header class="page-head">
    <div class="title-group">
      <h1 class="page-title">Nuevo <span class="accent">libro</span></h1>
      <p class="sub">Añade un volumen a «<%= @library.name %>».</p>
    </div>
  </header>

  <div class="card">
    <%= render "form" %>
  </div>
</section>
