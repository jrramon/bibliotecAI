# Backlog

Ideas pendientes, sin priorizar. Mover a slice/issue cuando se tomen.

## UX / web

- **Mejorar UX en móvil** en las páginas que aún quedan por revisar (~375-414px): **detalle de libro**, **wishlist** y **perfil**. La página de biblioteca y el header global ya están compactados.
- **Borrar notas propias**: el usuario debe poder borrar sus propios `UserBookNote`. Hoy no hay UI/endpoint.

## Compartir

- **Bibliotecas públicas / link de solo lectura**: que el dueño pueda compartir una biblioteca con un link de invitado (sin login) para que sólo la vea. Análogo al `public_wishlists` ya existente. Plan detallado en [`docs/plans/bibliotecas-publicas.md`](plans/bibliotecas-publicas.md).

## Monetización

- **Enlaces de afiliados** para comprar libros (Amazon, Bookshop.org…). Añadir botón «comprar» en la ficha de libro con `?tag=…`. Probablemente configurable por usuario o global.
