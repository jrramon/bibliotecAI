## Summary

<!-- 1-2 sentences: why this change, what it does. -->

## Slice / context

<!-- Link to docs/plans/<slug>.md if this PR is part of a planned slice. -->

## Test plan

- [ ] `bin/rails test` passes
- [ ] `bin/rails test:system` passes (if UI/system changes)
- [ ] `bundle exec standardrb` clean
- [ ] `bundle exec erb_lint --lint-all` clean
- [ ] `bundle exec brakeman --no-pager -q -c config/brakeman.yml` clean
- [ ] CHANGELOG.md updated under `## Unreleased`
- [ ] Manual smoke test done in browser (describe below)

<!-- Steps for a reviewer to verify the change manually. -->

## Screenshots / videos

<!-- For UI changes. Drop them here. -->
